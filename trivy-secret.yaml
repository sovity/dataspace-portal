allow-rules:
  - id: skip-sirius-docker-compose
    description: skip sirius docker compose secrets (we want to commit them)
    path: docs/deployment-guide/goals/sirius/remote/docker-compose.yaml
  - id: skip-e2e-docker-compose
    description: skip e2e docker compose secrets (we want to commit them as they are the same as the sirius ones)
    path: docs/deployment-guide/goals/e2e/docker-compose.yaml
